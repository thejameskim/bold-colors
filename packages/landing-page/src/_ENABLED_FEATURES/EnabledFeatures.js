/**
 * Enabled Features is a list of features that are enabled or disabled, so we can easily toggle features
 */
export const EnabledFeatures = {
	Colorify: false,
	GrayscaleIfNotColorify: true,
	Fade: true
}